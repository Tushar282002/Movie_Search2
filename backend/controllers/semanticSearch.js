const { getEmbedding, findSimilarDocuments } = require("../services/embed-functions");

const semanticSearch = async (req, res) => {
  // your existing semantic search code goes here
};

module.exports = semanticSearch;
